# Owner login (bootstraps the first users row). Never commit real values.
ADMIN_EMAIL=
ADMIN_PASSWORD=
# Optional. Defaults to Numan Usman on seed.
ADMIN_NAME=Numan Usman
# At least 16 characters. Use a long random string.
AUTH_SECRET=

# Neon nusmandotdev only. Do not prefix with VITE_.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
